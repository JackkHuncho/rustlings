// TODO: Change the line below to fix the compiler error.
const NUMBER:i8 = 3;
// constants need to have types, because they are allocated in the stack

fn main() {
    println!("Number: {NUMBER}");
}
